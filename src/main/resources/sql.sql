create table `user` (
    `name` varchar(255),
    `description` varchar(255),
    `address` varchar(255),
    `id` int(11) AUTO_INCREMENT PRIMARY KEY,
    `create_at` datetime,
    `dob` datetime
);



